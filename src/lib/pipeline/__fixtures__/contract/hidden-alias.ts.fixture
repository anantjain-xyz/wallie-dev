export async function hidePipelineWrite(admin: any) {
  const target = "sessions";
  const patch = { phase_status: "approved" };
  const query = admin.from(target);
  await query.update(patch).eq("id", "session-1");
}
