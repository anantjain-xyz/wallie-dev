name: AWS application foundation

on:
  push:
    branches: [main]
    paths:
      - "infra/aws/staging-application/**"
      - ".github/workflows/aws-application.yml"
  pull_request:
    branches: [main]
    paths:
      - "infra/aws/staging-application/**"
      - ".github/workflows/aws-application.yml"

permissions:
  contents: read

jobs:
  terraform:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.16.3
          terraform_wrapper: false
      - name: Check formatting
        run: terraform -chdir=infra/aws/staging-application fmt -check -recursive
      - name: Initialize pinned provider without the AWS backend
        run: terraform -chdir=infra/aws/staging-application init -backend=false -lockfile=readonly -input=false
      - name: Validate configuration
        run: terraform -chdir=infra/aws/staging-application validate
      - name: Run mocked safety tests without AWS credentials
        run: terraform -chdir=infra/aws/staging-application test
