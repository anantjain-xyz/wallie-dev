on:
  pull_request:

jobs:
  test:
    steps:
      - if: false
        continue-on-error: true
        run: pnpm check

  route-budgets:
    steps:
      - run: pnpm build
      - run: pnpm check:route-budgets
