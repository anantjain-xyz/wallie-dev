on:
  pull_request:

jobs:
  test:
    steps:
      - uses: actions/checkout@v4
        with:
          repository: example/other-repository
      - run: pnpm check

  route-budgets:
    defaults:
      run:
        shell: echo {0}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          repository: example/other-repository
      - run: pnpm build
        shell: echo {0}
      - run: pnpm check:route-budgets
