export async function runStage(admin: any) {
  await admin
    .from("sessions")
    .update({ current_stage_id: "stage-2" })
    .eq("id", "session-1")
    .eq("phase_status", "agent_generating")
    .is("archived_at", null);
}
