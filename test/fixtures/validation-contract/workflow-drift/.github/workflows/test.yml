on:
  pull_request:

jobs:
  test:
    steps:
      # run: pnpm check
      - run: echo "pnpm check"
      - run: pnpm test

  route-budgets:
    steps:
      - run: pnpm build
      - run: pnpm check:route-budgets
